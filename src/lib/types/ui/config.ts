export interface UserConfig {
  styles?: string;
  notifications?: number;
  showSpoilers?: boolean;
  userList?: boolean;
  recentEmojis?: string[];
  lastChannel?: number;
  hiddenCategories?: number[];
}
